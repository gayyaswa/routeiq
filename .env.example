# RouteIQ — environment configuration
# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real keys.

# ── Anthropic (default) ───────────────────────────────────────────────────────
LLM_PROVIDER=anthropic
LLM_MODEL=claude-sonnet-4-6
ANTHROPIC_API_KEY=sk-ant-...

# ── Nebius AI Studio (swap to this block to use Nebius) ──────────────────────
# Sign up at https://studio.nebius.ai — credits available on new accounts.
#
#LLM_PROVIDER=nebius
#LLM_MODEL=openai/gpt-oss-120b-fast
#NEBIUS_API_KEY=v1....
#NEBIUS_API_BASE=https://api.tokenfactory.nebius.com/v1/

# ── Rating providers (POI quality signals) ───────────────────────────────────
# TripAdvisor is the primary provider — higher quality reviews and photos.
TRIPADVISOR_API_KEY=
RATING_PROVIDER=tripadvisor

# Foursquare is the secondary swappable provider (set RATING_PROVIDER=foursquare to activate).
FOURSQUARE_API_KEY=
#RATING_PROVIDER=foursquare